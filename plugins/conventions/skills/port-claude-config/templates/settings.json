{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": {{PERMISSIONS}},
    "deny": ["Bash(git push --force*)", "Bash(git push -f*)"]
  },
  "enabledPlugins": {}
}
